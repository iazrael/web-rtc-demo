# Web RTC Demo

## 使用方法
1. 根目录执行 `npm install`
2. 根目录执行 `npm run build`
3. 到 server 目录执行 `npm install`
4. 到 server 目录执行 `npm run build`
5. 到 client 目录执行 `npm start`
6. 打开浏览器，访问 `http://localhost:3000`
